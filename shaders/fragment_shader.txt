# version 330 core
in vec3 position;
in vec2 texCoords;

out vec4 FragColor;

//uniform sampler2D diffuseMap;
uniform sampler2D texture0;
uniform sampler2D texture1;

void main() {
	//FragColor = vec4(position.x, 0.0, 0.0, 1.0);
	//FragColor = vec4(texCoords.y, 0.0, 0.0, 1.0);
	FragColor = texture(texture0, texCoords);
	//FragColor = vec4(texture(texture0, texCoords).rgb, 0.8); //vec4(0.5) * texture(texture2, texCoords);
}